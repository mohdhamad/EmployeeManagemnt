create database Employee;
use Employee;

create table signup(
name varchar(30),
fname varchar(30),
CourseName varchar(30),
CollegeName varchar(30),
DOB varchar(30),
Gender varchar(30),
emailaddress varchar(40),
Address varchar(40)
);
